# Changes

## v4 - 参考画像UI実装

- 指定された「おえかきジャンプ」イメージをHTML/CSS/SVGで再構築
- 黄色の背景、手描き風デコレーション、丸い白カード、パステル配色を再現
- カメラ/画像選択ボタンをピンク・ブルーの大型タイルへ変更
- 原画サンプル猫をHTML内のSVGで実装（画像貼り付けではありません）
- 6種類の動きアイコンをJavaScript生成SVGへ変更
- 動きの大きさ・速さをカード型スライダーに変更
- 作成CTA、鉛筆デコレーション、完成イメージカードを参考画像に寄せて再構築
- 完成前も結果イメージのプレースホルダーを表示し、生成後は実データに置き換え
- 既存の複数生成プロンプト管理・動きプロンプト管理機能は維持
- Service Workerキャッシュを更新

# 修整内容

## 1. カメラ
- 「カメラで撮る」を独立ボタン化。
- Android Chrome等では `navigator.mediaDevices.getUserMedia()` で背面カメラを直接起動。
- 撮影画面に「前/後を切替」「撮影」「閉じる」を追加。
- `getUserMedia()` が利用不可・権限拒否・端末非対応の場合、`capture="environment"` の標準カメラ入力へ自動フォールバック。

## 2. API設定
- メイン画面からAPIキー入力欄を削除。
- 右上の歯車「設定」ボタンから `settings.html` へ遷移。
- 設定画面でAPIキー、画像モデル、画質を設定。
- APIキーの表示/非表示切替を追加。
- APIキーの端末保存をON/OFFで選択可能。
- ONはlocalStorage、OFFはsessionStorageを使用。

## 3. プロンプト
- 設定画面に生成プロンプトの編集欄を追加。
- 初期プロンプトを表示した状態で自由に変更可能。
- 「初期設定に戻す」を追加。
- 保存したプロンプトを次回の画像生成時にそのまま利用。

## 4. アニメーション
- 1種類固定から複数選択式へ変更。
- ジャンプ、左右ゆれ、ふわふわ、くるくる、ぷるぷる、伸び縮みを追加。
- 複数選択した動きを同じGIFへ合成。
- 「動きの大きさ」「動きの速さ」を調整可能。
- 選択した動きを結果画面に表示。

## 5. GitHub Pages
- 全ファイルをリポジトリ直下へ置く構成。
- Pages設定は `main` + `/ (root)`。
- `index.html` をトップページにしてREADMEが表示される問題を回避。
- Service Workerキャッシュを更新し、旧版が残りにくいようネットワーク優先に変更。

## v3 UI / Prompt Manager

- トップ画面をモダン・ポップなカードUIへ刷新
- 生成プロンプトを複数保存・追加・複製・削除・切替できる管理画面を追加
- 動きプリセットを追加・複製・削除・修整できる管理画面を追加
- 各動きに「表示名」「アイコン」「アニメーションタイプ」「動きプロンプト」を設定可能
- 選択した動きプロンプトを画像生成時の追加指示として利用
- カスタム動きプリセットもメイン画面へ自動反映
- Service Workerキャッシュをv4へ更新

## v7 exact poster screen

- ユーザー提供 `oekaki_jump_web.zip` の `oekaki-jump.png` をメイン画面としてそのまま採用
- 画像の上にカメラ・画像選択・動き選択・スライダー・作成・保存の操作領域を重ねて既存機能と統合
- 選択画像と生成結果は参考画像内の該当エリアへオーバーレイ表示
- 設定画面の複数生成プロンプト管理・動きプロンプト追加/編集機能を維持
- Service Worker のキャッシュ対象に画面画像と `screen.css` を追加

## v8 hybrid UI
- 元の完成イメージ画像は静的な装飾・背景として保持
- 動き6タイルは元画像から切り出したビジュアル素材 + 実HTMLチェックボックスへ変更
- 選択状態の枠とチェックをHTML/CSSで表示
- 動きの大きさ/速さを実HTML rangeへ変更し、ノブと色付きトラックが操作に追従
- 生成結果を元画像のPNG/GIFカード位置に重ねて表示
- 保存ボタンを実リンクとして表示し、PNG/GIFのdownload属性を明示
- APIキー、画像、動きが揃うまで作成ボタンを無効化
